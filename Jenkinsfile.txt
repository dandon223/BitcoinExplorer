pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                dir("blockchainProject")
                {
                    sh 'mvn clean deploy'
                }
                dir("pik/blockchainProject/src/main/ui/front_explorer")
                {
                    sh 'ls'
                    
                }

                
                
            }
        }
        
    }
}
